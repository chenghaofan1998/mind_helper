# Action Pocket P0 收口清单

> 目标：先证明“热键打开小窗 → 记入 → 查询 → 使用原文 → 隐藏”在真实环境中可靠，再启动 P1 截图能力。
>
> 状态标记：`[x]` 已有自动证据；`[ ]` 待完成。涉及真实 Graph 时请先备份，并使用不含敏感信息的测试内容。

## 一、自动化与代码收口（AI 执行）

- [x] 当前单元与集成测试通过（包含默认写入、多项目隔离、单文件边界、locate API 与纯生命周期状态机）。
- [x] 前端类型检查与生产构建通过。
- [x] 独立本机服务构建通过。
- [x] 使用临时 Graph 完成生产服务写入/查询冒烟闭环：HTTP 200，原文可找回。
- [x] P0 v2 图片与提示词清单一致：01×2、02×3、03×3、04×2。
- [x] 统一设计文档中的图片身份、P0/P1/P2 范围和截图流程。
- [x] 修复 Neutralino 静态页面与知识 API 不同源的问题：启动器让桌面壳加载本机服务页面。
- [x] 实现最小 Windows 启动器：启动本机服务、等待就绪、启动桌面壳、退出时清理子进程。
- [x] 实现 `Ctrl+Alt+P` 全局热键注册；launcher 缓存并校验 shell HWND，hidden/minimized/exited 均走统一恢复逻辑，失败时保留托盘入口。
- [x] Neutralino 使用透明无边框窗口；launcher 对 HWND 优先应用 Windows 11 Mica、失败时降级 Windows 10 Acrylic，CSS 提供半透明 blur 与高对比/无 blur 降级。
- [x] 托盘一级菜单收敛为显示 / 隐藏 / 设置… / 退出；添加文件夹与添加 Markdown 不再铺在托盘一级。
- [x] 实现与主浮窗分离的独立原生 WinForms 设置窗口：查看项目清单、添加文件夹、添加单个 `.md`/`.markdown`、把文件夹标记为普通 Markdown 或 Logseq、重命名、删除（删除最后一项二次确认）、设置默认项目、保存/取消。
- [x] 设置窗文件/目录选择框以设置窗为 owner；修改先暂存，保存后才原子写入 `projects.v1.json` 并受控重启服务与小窗，取消不改运行配置。
- [x] 主窗移除“添加项目”加号与底部“设置”按钮；单项目为紧凑标签、多项目为 `select`，无项目时只提示“请从系统托盘打开设置”。
- [x] 主窗代码已改为三段结构（固定顶部输入/项目、中间唯一滚动结果区、底部固定操作区），结果卡操作使用同高同宽的 grid；实际窗口下是否完全不错位仍以第四节真机视觉验收为准。
- [x] 已实现 Today AI 白色毛玻璃视觉 token：header 不再深色，主壳 white/70、14px blur、26px 圆角，背景只留轻蓝/桃漫反射；实际 Mica/Acrylic 呈现仍以第四节真机视觉验收为准。
- [x] 按职责拆分启动器源文件（项目配置/设置窗、桌面窗口与 backdrop、launcher 生命周期、服务启动、自检），每个文件 <500 行；`native/build.ps1` 编译 `native/*.cs` 全部源文件。
- [x] 更新打包脚本，将 Node 运行时、服务产物、Web 资源和桌面壳纳入同一 Windows 包。
- [x] 为启动器增加不依赖 UI 的自检，并更新构建/打包说明。
- [x] 改动后重新运行完整测试套件、类型检查、Vite 构建和差异格式检查。
- [ ] 在 Windows 编译启动器并执行自检。当前 Linux 环境无 Windows `csc.exe`；已用本机 .NET SDK 的 net48 参考程序集交叉编译全部 `native/*.cs`，结果为 0 error / 0 warning，但热键、托盘菜单、设置窗控件与 Mica/Acrylic 行为仍需真机验收。

## 二、真实知识库验收（用户执行）

准备：选择一个已备份的真实文件型 Graph，并记下绝对路径。

- [ ] 将 Graph 路径提供给构建/启动命令，确认应用只访问该目录。
- [ ] 记入一段唯一测试文本，确认 Markdown 中原文、路径和行号正确。
- [ ] 用自然语言找回该文本，确认结果不超过 5 条并包含路径、行号、版本和必要上下文。
- [ ] 将测试目录改为只读后再次保存，确认不显示成功且输入仍保留；完成后恢复权限。
- [ ] 查询一条危险命令，例如 `git reset --hard HEAD~1`，确认首次复制必定进入确认框，且应用没有执行命令。
- [ ] 添加目录 A/B 与单 Markdown 文件 C（通过托盘“设置…”），切换后确认相同查询、默认写入和草稿都不会跨项目；单文件项目不得读取或创建兄弟文件。
- [ ] 对检索结果使用“打开原文”，确认 Windows 文件管理器真实选中文件；文件移动、恶意定位与不支持 locate 的 Connector 均明确降级。
- [ ] 在未配置 Graph 的情况下启动一次，确认界面明确显示“未配置知识源”。

请记录：操作系统版本、Graph 类型（Logseq 文件型/普通 Markdown/其他）、成功项、失败步骤和截图。

## 三、Windows 桌面真机验收（用户执行）

> 这些项目依赖 Windows GUI、系统热键和打包产物，当前 Linux 开发环境无法代替真机结论。

- [ ] 运行 Windows 构建脚本，确认启动器自检通过并生成发布包。
- [ ] 在未预先安装 Node.js 的测试机上解压并启动，确认无需命令行即可使用。
- [ ] 冷启动和已驻留两种情况下各测 5 次 `Ctrl+Alt+P` 到输入框可输入的耗时；目标每次 `< 300ms`。
- [ ] 热键已被其他程序占用时，确认出现明确提示，托盘“打开 Action Pocket”仍可用。
- [ ] 连续 20 次交叉验证热键/托盘/Esc 隐藏恢复；再验证最小化恢复、X 退出 shell 后重启、置顶切换和全局退出后无残留进程。
- [ ] 验证中文/空格路径“打开原文”；无法打开时给出明确失败提示，不把复制定位伪装为打开成功。
- [ ] 重启 Windows 后重复启动一次，确认端口冲突、旧进程和路径含中文/空格时行为正常。

请回传：发布包是否启动、10 次耗时、失败截图，以及任务管理器中退出后的进程情况。

## 四、视觉评审（用户确认）

现存 10 张 v2 PNG 是蓝灰 token 更新前的历史设计样例，不是当前视觉或实现验收截图。重新生成后再确认：

- [ ] Win11 Mica、Win10 Acrylic 与 CSS fallback 是否保持同一个清透白色毛玻璃外壳：header 不是深色条，主壳 white/70、24–28px 圆角；无系统最小化/最大化/关闭框且 header 可拖动。
- [ ] 主窗是否只出现紧凑项目切换、输入/结果与主要操作；项目增删改是否都在托盘“设置…”独立窗口，且主窗没有加号/设置按钮。
- [ ] 查询前后、空结果、多卡片、长标题/长路径、危险提示下，底部主操作和结果卡复制/打开是否保持同高同宽、不错位不遮挡。
- [ ] 原文是否明显高于装饰、状态和次要操作。
- [ ] 危险确认是否足够明确，默认安全路径是否容易识别。
- [x] 旧版 01–10 PNG 已删除，仅保留文字评审记录。

已知样例限制：AI 生图中的图标、焦点和文字可能与真实交互不完全一致，最终以代码行为和真机验收为准。

## 五、真实使用验证（用户执行）

P0 真机通过后，连续使用 3–5 天，只记录以下数据：

- [ ] 每天主动打开次数。
- [ ] 记入成功/失败次数。
- [ ] 查询后找到可用原文的次数。
- [ ] 相比直接打开原知识库是否更快。
- [ ] 是否出现“问题就在屏幕上、很想直接截图”的真实场景及次数。

只有截图需求出现稳定频次后，才批准 P1；不要用新增功能掩盖记入、检索或呈现问题。
