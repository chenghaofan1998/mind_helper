# Command Pocket v4 · 进度盘点（做了什么 / 没做什么）

> 更新于 2026-09-07（本分支）。人话版见文末"一句话状态"；权威规格见 `docs/PRODUCT.md`。

## 一、已做（✅ 完成并提交）

### 文档
| 文档 | 内容 | 状态 |
|---|---|---|
| docs/PRODUCT.md | **核心文档·唯一权威规格**：定位/P0 需求/核心流程/数据模型/功能全集 §6/验收 | ✅ |
| docs/REQUIREMENTS.md | 需求裁定（多 PM 场景→需求→解法）+ 三条拍板 + PM3 人工补录 | ✅ |
| docs/TESTCASES.md | 场景测试用例集（素材/步骤/期望），A/B/G 组当前可测 | ✅ |
| docs/SCENARIO.md · VISION*.md · MASTER/MVP | 推导过程与历史（冲突时以 PRODUCT.md 为准） | 存档 |

### 代码（均已在 main 提交）
| 提交 | 内容 |
|---|---|
| `705bd2b` · **6.1a/M0a** | CommandCard 新增字段：`purpose`(目的句) / `rs rm rr`(风险三件) / `rlabeled`(是否标注)；WriteCard/ReadCard **宽容读写**（旧库无字段自动补默认：rr=unknown、rlabeled=false）；危险复制确认初版 |
| `643388c` · **6.3a** | 目的句进入检索池并加权（目的句 ≥ 标题）；新增 `scripts/spec-search.js` 逻辑对拍台 |
| `adf8d5a` · **审查修复** | fav 前显式逗号（保证 JSON 有效）；`DangerGuard` 公共危险确认（**小窗与后台库一致拦截**）；未知 key 裸值宽容跳过；导入编辑器文案对齐（"高/极高复制前会确认"）；对拍台术语词典补齐 15 组 |

### 验证（本环境已完成的部分）
- Node 逻辑对拍台 **12/12 全绿**：整句用话找回第 1 位 ✓ 碎片命中 ✓ 别名/术语组回归（端口/lsof/复制/日志）✓ 目的句权重≥标题 ✓ 中文一句话兜底 ✓ 空查询预测非空 ✓ 零命中返回空 ✓ 产品过滤 ✓
- 审查 agent 一轮静态审计已闭环：高危 JSON 隐患、后台库绕过确认、未知键解析、文案矛盾——**全部修复并复核**

## 二、没做 / 进行中（⏳）

### 按 PRODUCT.md §6 功能全集
- ⏳ **6.1 剩余**：RegisterHotKey 返回值检查；CommandStore 批量写（BumpCards）
- ⏳ **6.2 记一笔 UI**：小窗/后台录入收敛成"目的句(必填)+风险三件"，复制内容预填剪贴板——**下一个开工项**
- ⏳ **6.3 剩余（UI 层）**：首屏"最可能1+备选≤3"呈现；零命中出口（明示+一键转记一笔）；空库首开引导；碎片容错的意图区
- ⏳ **6.4 风险展示**：结果项危险色（红/黄未标注/人工标绿）；卡面三行（动什么/改删什么/能否还原）
- ⏳ **6.5 试用态与自洁**：lifecycle 字段、30/60/90 惰性计算、归档/待清理
- ⏳ **6.6 可观测**：metrics.jsonl 埋点（唤起/采纳/零命中/Esc）
- ⏳ **6.7 SelfTest 扩充**：把对拍台用例转成 C# 断言并入 --self-test（A1-A5/B3/E2 等）
- 注：审查提示"预览复制路径（L1596 附近）"是否同样需危险确认——列入 6.2 一并处理

### 后置（明确不做，写进 PRODUCT §6.8）
Logseq 双向桥（P1）· 整库呈现/教学（P2）· 周姐成品卡（P1）· 执行引擎 · AI 兜底 · 跨设备

## 三、阻塞项（⚠️ 需要你侧配合）

1. **Windows 编译验证**：本环境为 Linux 容器（无 .NET），代码只能静态审查 + 逻辑对拍；最终须在你 Windows 执行 `npm run desktop`（编译 + --self-test 全绿）+ 手工用例（docs/TESTCASES.md）
2. **代码同步/上传**：本环境无 ssh 与 GitHub 凭据，无法 `git push`；新分支已本地建好，推送需在你 `F:\txt_parter` 侧执行（命令见下）
3. 微信文件通道近期不稳定（大文件/部分 .md 发送失败）——如需完整源码请走 git 或告知替代通道

## 四、下一步（按序）

1. 6.2 记一笔 UI（含预览复制危险确认补齐）
2. 6.4 风险展示（危险色/三行）
3. 6.3 UI 层（首屏预测/零命中出口/空态引导）
4. 6.5 试用态 · 6.6 埋点 · 6.7 SelfTest 扩充
5. Windows 端 `npm run desktop` + TESTCASES 全量验收 → 试点

## 五、一句话状态（人话）

> 图纸画完了（核心文档+测试清单）；地基和"认人话的搜索"已做并自测通过；正在做"当场记一笔"的界面；代码还没在你 Windows 上编译验证过，也没推到 GitHub（本环境推不了，需要你那边执行）。

## 附：分支与推送

```bash
# 本仓库当前
git branch -a            # main（本地已含 v4 阶段提交） + 本分支 feature/v4-p0-kernel
# 在你 F:\txt_parter 推送新分支（本地有凭据时）
git push -u origin feature/v4-p0-kernel
# 若你本地 main 落后，先合并或直接拉取：
# git fetch origin && git checkout -b feature/v4-p0-kernel origin/main
```
